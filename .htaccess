<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Si la solicitud no es a un archivo físico y no es un directorio,
    # sirve el index.html de tu SPA.
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /index.html [L]
</IfModule>